/**
 * Generated by Orval from apps/push-gateway/openapi/admin-v1.yaml.
 * Do not edit manually. Run pnpm nx run push-gateway-ui:generate-api.
 */
import { HttpClient } from '@angular/common/http';

import { Injectable, inject } from '@angular/core';

import { Observable } from 'rxjs';

import type { OperatorSession } from '../admin-api.schemas';

@Injectable({ providedIn: 'root' })
export class SessionService {
  private readonly http = inject(HttpClient);
  /**
   * Returns the authenticated Operator Identity and current Operator Session.
   * No cookie value, XSRF value, provider token, or raw provider claim is
   * returned in the document.
   * @summary Get the current Operator Session
   */

  getSession<TData = OperatorSession>(): Observable<TData> {
    return this.http.get<TData>(`/admin/api/v1/session`);
  }
}

export type GetSessionClientResult = NonNullable<OperatorSession>;
