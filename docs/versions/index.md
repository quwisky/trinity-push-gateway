# Documentation versions

Use **Latest** for the newest stable gateway release. Use **Next** only when
evaluating changes already merged to `master` but not yet released. Immutable
`vX.Y.Z` snapshots remain available for deployed versions.

The version selector above the page content reads the local version manifest;
it never contacts an external search or analytics service.
