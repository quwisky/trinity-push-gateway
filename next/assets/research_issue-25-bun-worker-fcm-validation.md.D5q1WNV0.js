import{_ as t,o as a,c as o,a5 as s}from"./chunks/framework.UjrY2eRK.js";const p=JSON.parse('{"title":"Issue 25: Bun metrics Worker and Firebase validation","description":"","frontmatter":{},"headers":[],"relativePath":"research/issue-25-bun-worker-fcm-validation.md","filePath":"research/issue-25-bun-worker-fcm-validation.md","lastUpdated":1788169631000}'),i={name:"research/issue-25-bun-worker-fcm-validation.md"};function r(n,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="issue-25-bun-metrics-worker-and-firebase-validation" tabindex="-1">Issue 25: Bun metrics Worker and Firebase validation <a class="header-anchor" href="#issue-25-bun-metrics-worker-and-firebase-validation" aria-label="Permalink to &quot;Issue 25: Bun metrics Worker and Firebase validation&quot;">​</a></h1><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Ship the metrics writer as a second, explicit Bun bundle entry point and copy the complete Bun output directory into the runtime image. Reference its emitted <code>.js</code> name from both source and production, attach <code>error</code> and <code>close</code> handlers before posting work, and use <code>ref: false</code> plus a bounded cooperative shutdown. Worker loss disables metrics collection and drops subsequent increments; it must not affect the notification path.</p><p>Validate Firebase with one direct HTTP v1 request whose JSON body has the top-level <code>validate_only: true</code> flag and a fixed, deliberately invalid target. Treat only a successful response or an FCM-specific invalid-target response as proof that the configured credentials could access the configured project&#39;s FCM API. Report it strictly as credential/project/API access, never as an end-to-end delivery result.</p><h2 id="bun-1-4-worker-and-bundle-contract" tabindex="-1">Bun 1.4 Worker and bundle contract <a class="header-anchor" href="#bun-1-4-worker-and-bundle-contract" aria-label="Permalink to &quot;Bun 1.4 Worker and bundle contract&quot;">​</a></h2><p>Bun&#39;s <code>Worker</code> is an experimental server-side Web Worker implementation. A worker may use ES modules without <code>{ type: &quot;module&quot; }</code>; Bun&#39;s 1.4 declarations state that the <code>type</code> option does nothing (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/packages/bun-types/bun.d.ts#L393-L397" target="_blank" rel="noreferrer">Bun 1.4 Worker options</a>). Bun emits <code>open</code> when the worker is ready, queues messages sent before <code>open</code>, and emits <code>error</code> when the entry module cannot resolve (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/runtime/workers.mdx#L13-L59" target="_blank" rel="noreferrer">Bun 1.4 Worker creation</a>, <a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/runtime/workers.mdx#L99-L111" target="_blank" rel="noreferrer">open semantics</a>). The same page defines the relevant lifecycle behavior:</p><ul><li>a worker remains alive while its event loop has work, including a global <code>message</code> listener;</li><li><code>terminate()</code> requests exit as soon as possible;</li><li><code>close</code> is emitted when Bun has marked the worker terminated, before full teardown is necessarily complete, and carries the worker exit code;</li><li><code>process.exit()</code> in the worker exits only that worker; and</li><li><code>ref: false</code>/<code>unref()</code> prevents the worker from keeping the parent process alive.</li></ul><p>Those lifecycle details are in the tagged <a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/runtime/workers.mdx#L196-L225" target="_blank" rel="noreferrer">termination</a> and <a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/runtime/workers.mdx#L227-L259" target="_blank" rel="noreferrer">reference-management</a> documentation. Use <code>ref: false</code> explicitly: Bun 1.4&#39;s prose and type annotation disagree about the default, so relying on it would make shutdown behavior ambiguous.</p><p>For bundling, Bun generates one output for every explicit entry point (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/bundler/index.mdx#L199-L219" target="_blank" rel="noreferrer">Bun 1.4 entry-point contract</a>). It does not document ordinary <code>bun build</code> as discovering a <code>new Worker(...)</code> target. Its standalone-executable guidance is explicit that the worker must be added as another build entry point (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/bundler/executables.mdx#L601-L638" target="_blank" rel="noreferrer">Bun 1.4 Worker executable guidance</a>).</p><p>An exact local probe with the repository-pinned Bun 1.4.0 confirmed the same constraint for the current non-compiled build:</p><ol><li>Building only <code>main.ts</code>, where the main module constructs <code>new Worker(new URL(&quot;./worker.ts&quot;, import.meta.url).href)</code>, emitted only <code>main.js</code>; the metafile contained no worker input or output.</li><li>Building <code>main.ts worker.ts</code> emitted <code>main.js</code> and <code>worker.js</code>, but did not rewrite the literal <code>./worker.ts</code> in <code>main.js</code>. Running the bundle therefore emitted <code>error</code> and then <code>close</code> with exit code 1 because <code>worker.ts</code> was not present.</li><li>Running the source modules directly delivered the queued message, and <code>terminate()</code> produced <code>close</code> with exit code 0. A deliberately throwing worker produced <code>error</code> followed by <code>close</code> with exit code 1 while the handled failure did not terminate the parent.</li><li>Changing the constructor URL to <code>./worker.js</code> while retaining <code>worker.ts</code> made both the direct source run and the two-entry production bundle deliver the message and close with exit code 0.</li></ol><h3 id="implementation-consequences" tabindex="-1">Implementation consequences <a class="header-anchor" href="#implementation-consequences" aria-label="Permalink to &quot;Implementation consequences&quot;">​</a></h3><ul><li><p>Add the metrics module as a second entry point in both the Nx Bun build and Docker build. Do not expect the constructor expression to make it reachable. Set <code>--root src/bun</code> so the default <code>[dir]/[name].[ext]</code> entry naming produces a deliberate, stable relative layout (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/bundler/index.mdx#L935-L980" target="_blank" rel="noreferrer">Bun 1.4 root and naming</a>).</p></li><li><p>Construct it as:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Worker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> URL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./metrics-writer.worker.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">meta</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.url).href, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;metrics-writer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ref: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Keep the source module as <code>metrics-writer.worker.ts</code>. Bun substitutes <code>.js</code> with the matching <code>.ts</code> module when the JavaScript file is absent during source execution (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/runtime/module-resolution.mdx#L62-L75" target="_blank" rel="noreferrer">Bun 1.4 module substitution</a>); the literal <code>.js</code> then matches the emitted production entry point.</p></li><li><p>Copy the whole Bun bundle directory into the runtime image, not only <code>main.js</code>. This includes the worker and remains correct if code splitting is enabled later. Bun currently defaults splitting to <code>false</code> (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/bundler/index.mdx#L425-L484" target="_blank" rel="noreferrer">Bun 1.4 splitting contract</a>).</p></li><li><p>Gate the bundle with its metafile and a production-artifact smoke test: the worker entry must appear as both an input and an output, and the copied runtime layout must reach <code>open</code> or complete one test flush without an <code>error</code>/non-zero <code>close</code> (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/docs/bundler/index.mdx#L1335-L1428" target="_blank" rel="noreferrer">Bun 1.4 metafile contract</a>).</p></li><li><p>Register lifecycle listeners before posting the first message. On <code>error</code> or unexpected <code>close</code>, atomically mark the producer dead and replace it with a no-op drop sink. Retain only a sanitized <code>event.message</code>: Bun&#39;s tagged test permits an uncaught worker failure&#39;s <code>event.error</code> to be <code>null</code> (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/test/js/web/workers/worker.test.ts#L364-L371" target="_blank" rel="noreferrer">Bun 1.4 error test</a>). Do not use <code>postMessage()</code> as a liveness check because it is a silent no-op after termination (<a href="https://github.com/oven-sh/bun/blob/bun-v1.4.0/test/js/web/workers/worker.test.ts#L398-L405" target="_blank" rel="noreferrer">Bun 1.4 termination test</a>). Do not synchronously restart, wait for, or back-pressure Notification Requests. At service shutdown, request a cooperative stop/ack with a short bound, then call <code>terminate()</code> only as the fallback; <code>terminate()</code> returns <code>void</code> and <code>close</code> does not prove full teardown is already complete.</p></li></ul><h2 id="fcm-http-v1-validate-only-contract" tabindex="-1">FCM HTTP v1 <code>validate_only</code> contract <a class="header-anchor" href="#fcm-http-v1-validate-only-contract" aria-label="Permalink to &quot;FCM HTTP v1 \`validate_only\` contract&quot;">​</a></h2><p>The send method is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>POST https://fcm.googleapis.com/v1/projects/{project-id}/messages:send</span></span></code></pre></div><p>Its request schema places <code>validate_only</code> at the top level, as a sibling of the required <code>message</code> object—not inside <code>message</code> (<a href="https://firebase.google.com/docs/reference/fcm/rest/v1/projects.messages/send" target="_blank" rel="noreferrer">FCM send reference</a>):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;validate_only&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;trinity-push-gateway-validation-only&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The reference defines <code>validate_only</code> as testing the request without actually delivering it. Firebase&#39;s Admin SDK describes the corresponding dry-run mode as performing SDK and backend validation and emulating the send without delivery (<a href="https://github.com/firebase/firebase-admin-go/blob/eebb06f2a643fbb59b1cb262874a943584475128/messaging/messaging.go#L973-L986" target="_blank" rel="noreferrer">Firebase Admin Go implementation</a>). Only one message target is required; other payload fields are optional, so the minimal target-only message avoids unrelated platform or payload validation (<a href="https://firebase.google.com/docs/reference/fcm/rest/v1/projects.messages#resource:-message" target="_blank" rel="noreferrer">FCM Message schema</a>).</p><p>The call must use an OAuth access token with the <code>https://www.googleapis.com/auth/firebase.messaging</code> scope and the token must be sent as <code>Authorization: Bearer ...</code>. The configured project identifier belongs in the endpoint, and the calling service account needs permission in that target project (<a href="https://firebase.google.com/docs/cloud-messaging/send/v1-api#authorize-http-v1-send-requests" target="_blank" rel="noreferrer">FCM HTTP v1 authorization</a>).</p><p>The current REST reference marks <code>message.token</code> deprecated in favor of <code>fid</code> while explicitly retaining token/FID compatibility during the transition. This ticket&#39;s probe can still use the registration-token field required by its accepted contract; it must not silently migrate the delivery contract.</p><h3 id="result-classification" tabindex="-1">Result classification <a class="header-anchor" href="#result-classification" aria-label="Permalink to &quot;Result classification&quot;">​</a></h3><p>Use a fixed sentinel that is deliberately not an actual Push Key. It is not a secret and must never be replaced with a Client Installation&#39;s real token. Interpret the result narrowly:</p><table tabindex="0"><thead><tr><th>Response</th><th>Validation result</th></tr></thead><tbody><tr><td><code>2xx</code></td><td>Access succeeded; no delivery occurred because <code>validate_only</code> was true. A success for the deliberately invalid sentinel is unexpected but still not a delivery result.</td></tr><tr><td>FCM-specific <code>INVALID_ARGUMENT</code> for the registration token, or <code>UNREGISTERED</code></td><td>Access succeeded. OAuth minting, the configured project route, authorization, and FCM request processing completed far enough for FCM to classify the synthetic target.</td></tr><tr><td><code>google.rpc.BadRequest</code> field violation</td><td>Failed validation request; do not confuse a malformed payload with the expected invalid-target result.</td></tr><tr><td>OAuth failure, <code>401</code>, <code>403</code>, or other project/permission response</td><td>Credential, project, API-enable, or permission validation failed.</td></tr><tr><td>Timeout, network failure, <code>429</code>, or <code>5xx</code></td><td>Access could not be established now; report a coarse unavailable/failed reason, not credential success.</td></tr></tbody></table><p>FCM&#39;s official error example distinguishes an invalid registration token by the <code>details[].@type</code> value <code>type.googleapis.com/google.firebase.fcm.v1.FcmError</code> and its <code>errorCode</code>, while a malformed request uses <code>google.rpc.BadRequest</code> (<a href="https://firebase.google.com/docs/cloud-messaging/error-codes#rest_error_codes_for_the_http_v1_api" target="_blank" rel="noreferrer">FCM HTTP v1 error format</a>). Inspect those structured fields rather than response text. The same source documents <code>UNREGISTERED</code> as HTTP 404 and explains that <code>INVALID_ARGUMENT</code> can also mean a malformed message; the FCM-specific detail and deliberately minimal payload are therefore essential.</p><p>Treating the exact FCM-specific invalid-target response as access success is an operational inference from reaching FCM&#39;s documented target-classification path instead of receiving an OAuth, project, API-enable, or permission error. The public API documentation does not promise the backend&#39;s internal validation order. Keep the surfaced claim narrow and retain contract tests for every response class.</p><p>This probe establishes only that the configured service-account credential can mint a scoped token and reach the configured project&#39;s enabled/authorized FCM send API. It does not test an actual app registration, Sender ID ownership, APNs setup, FCM handoff to a Client Installation, notification rendering, or device delivery. Those claims require a real installation and are explicitly outside this safe operator action.</p>`,27)])])}const m=t(i,[["render",r]]);export{p as __pageData,m as default};
